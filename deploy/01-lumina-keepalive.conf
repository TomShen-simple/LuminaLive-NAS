# Reap unresponsive SSH peers so abandoned reverse forwards release their ports.
# This does not impose an idle timeout on clients that respond to keepalives.
ClientAliveInterval 60
ClientAliveCountMax 3
